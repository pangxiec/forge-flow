# HTML Output Rules

- Output a complete single HTML document.
- Use inline CSS and local JavaScript only.
- Do not use external CDN, remote images, remote fonts, fetch, axios, XMLHttpRequest, or module imports.
- The document must be previewable inside an iframe through srcdoc.
- Start with <!doctype html> or <html>.
- Do not output Markdown fences, JSON, or explanations.
